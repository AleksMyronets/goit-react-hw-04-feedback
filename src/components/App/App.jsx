import { Feedback } from '../FeedbackWidget/FeedbackWidget';
import { Container } from "./App.styled"

export const App = () => {
  return (
    <Container>
      <Feedback />
    </Container>
  );
};